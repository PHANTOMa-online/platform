apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: metallb
  namespace: metallb
spec:
  values:
    configInline:
      peers:
        - peer-address: 10.16.0.1
          peer-asn: 64512
          my-asn: 64513
      address-pools:
        - name: default
          avoid-buggy-ips: true
          protocol: bgp
          addresses:
          - 10.18.0.0/16
